<!DOCTYPE html="html">
<html>

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" />
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js"></script>
    <script src="https://use.fontawesome.com/releases/v5.0.8/js/all.js"></script>
    <link rel="stylesheet" href="/css/teacher.css">
</head>

<body>
    <a href="/auth/logout">SAIR</a>
    <div class="container-fluid">
        <div class="row">
            <div class="col-5">
                <div id="subjects">
                    <h2>
                        Matérias
                    </h2>
                    <table id="subjects-table">
                        <thead>
                            <tr>
                                <th>Nome</th>
                            </tr>
                        </thead>
                        <tbody></tbody>
                    </table>
                </div>
                <div id="subject-info"></div>
                <br>
                <div id="subject-exercises"></div>
            </div>
            <div class="col-6">
                <h2>
                    Exercícios
                </h2>
                <h4>Adicionar exercício:</h4>
                <form action="/teacher/exercises/create" method="POST">
                    <input type="text" name="name" placeholder="Nome" required="required">
                    <br>
                    <br>
                    <select name="subject" id="select-subject"></select>
                    <br>
                    <br>
                    <textarea name="question" cols="30" rows="5" placeholder="Questão" required="required"></textarea>
                    <p>
                        Linguagem : python(única suportada até agora)
                    </p>
                    <textarea name="inputs" cols="30" rows="5" placeholder="Inputs (separados por @)"
                        required="required"></textarea>
                    <br>
                    <br>
                    <textarea name="outputs" cols="30" rows="5" placeholder="Outputs (separados por @)"
                        required="required"></textarea>
                    <br>
                    <button type="submit">Adicionar</button>
                </form>
            </div>
        </div>
    </div>
    <script src="scripts/teacher.js"></script>
</body>

</html>